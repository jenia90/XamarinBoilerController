<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BoilerController"
             x:Class="BoilerController.MainPage">

    <StackLayout Orientation="Vertical">
        <Label Text="Welcome to Xamarin Forms!" 
               VerticalOptions="Center" 
               HorizontalOptions="Center" />
        <StackLayout Orientation="Horizontal" VerticalOptions="Center" HorizontalOptions="Center">
            <Label Text="Current Status: "/>
            <Label Text="{Binding Path=Status}" TextColor="{Binding Path=StatColor}"/>
        </StackLayout>
        <Button Text="Switch Boiler" Command="{Binding Path=SwitchCommand}"/>
        <Label Text="Pick start date and time:"/>
        <DatePicker Date="{Binding Path=DateTime}"/>
    </StackLayout>

</ContentPage>